<!-- i18n:manual -->
# A2A — протокол «агент — агент»

> MCP — это связка «агент — инструмент». A2A (Agent2Agent) — связка «агент — агент»: открытый протокол, который позволяет непрозрачным агентам на разных фреймворках работать вместе. Google выпустила его в апреле 2025-го, в июне 2025-го передала Linux Foundation, в апреле 2026-го протокол дошёл до v1.0 при 150+ поддержавших организациях, среди которых AWS, Cisco, Microsoft, Salesforce, SAP и ServiceNow. Он вобрал в себя ACP от IBM и получил платёжное расширение AP2. В этом уроке разбираем Agent Card, жизненный цикл Task и две транспортные привязки.

**Type:** Build
**Languages:** Python (stdlib, Agent Card + Task harness)
**Prerequisites:** Phase 13 · 06 (MCP fundamentals), Phase 13 · 08 (MCP client)
**Time:** ~75 minutes

## Learning Objectives

- Различать сценарии «агент — инструмент» (MCP) и «агент — агент» (A2A).
- Публиковать Agent Card по адресу `/.well-known/agent.json` — со списком skills и метаданными эндпоинта.
- Проходить жизненный цикл Task (submitted → working → input-required → completed / failed / canceled / rejected).
- Использовать Messages с Parts (text, file, data) и Artifacts как результат работы.

## The Problem

Агент службы поддержки должен передать написание отчёта отдельному агенту-писателю. Варианты до появления A2A:

- Самописный REST API. Работает, но каждая пара агентов — отдельная разовая интеграция.
- Общая кодовая база. Требует, чтобы оба агента жили на одном фреймворке.
- MCP. Не подходит: MCP нужен для вызова инструментов, а не для двух агентов, которые работают вместе и при этом сохраняют непрозрачность своих внутренних рассуждений.

A2A закрывает этот пробел. Он описывает взаимодействие так: один агент отправляет другому Task, у которого есть жизненный цикл, сообщения и артефакты. Внутреннее состояние вызванного агента остаётся непрозрачным — вызывающий видит только переходы состояний задачи и итоговые результаты.

A2A — это протокол «пусть агенты с разных фреймворков разговаривают друг с другом». Он не заменяет MCP; они дополняют друг друга.

> 🎒 **На пальцах.** Представьте, что каждой паре агентов нужен свой переходник: 5 агентов, каждый умеет звать каждого — это 5×4 = 20 самописных интеграций. A2A превращает это в 5 карточек, по одной на агента, и ноль работы на каждую новую пару. Ровно та же логика, что и у MCP с матрицей N×M, только этажом выше.

## The Concept

### Agent Card

Каждый агент, совместимый с A2A, публикует карточку по адресу `/.well-known/agent.json`:

```json
{
  "schemaVersion": "1.0",
  "name": "research-agent",
  "description": "Summarizes academic papers and drafts citations.",
  "url": "https://research.example.com/a2a",
  "version": "1.2.0",
  "skills": [
    {
      "id": "summarize_paper",
      "name": "Summarize a paper",
      "description": "Read a paper PDF and produce a 3-paragraph summary.",
      "inputModes": ["text", "file"],
      "outputModes": ["text", "artifact"]
    }
  ],
  "capabilities": {"streaming": true, "pushNotifications": true}
}
```

Обнаружение работает через URL: забираете карточку, узнаёте URL A2A-эндпоинта, перечисляете skills.

> 🎒 **На пальцах.** Agent Card — это визитка на двери офиса: имя, что умеем, куда стучаться. В примере выше skill ровно один — `summarize_paper`, он принимает `text` и `file`, а отдаёт `text` и `artifact`. Значит PDF ему прислать можно, а картинку уже нет: `inputModes` этого не обещает, и вызывающий узнаёт это до первого запроса.

### Signed Agent Cards (AP2)

Расширение AP2 (сентябрь 2025) добавляет к Agent Card криптографические подписи. Издатель подписывает свою карточку через JWT, а потребители проверяют подпись. Это закрывает подмену агента чужим именем.

> 🎒 **На пальцах.** Подпись — как голограмма на паспорте. Без AP2 кто угодно поднимает сервер, отдаёт карточку с именем `payments-agent`, и ваш агент отправляет ему задачу с реквизитами. С подписью JWT вы сверяете издателя карточки с известным ключом, и подделка отваливается на этапе проверки, а не после списания денег.

### Task lifecycle

```
submitted -> working -> completed | failed | canceled | rejected
             -> input_required -> working (loop via message)
```

Клиенты стартуют вызовом `tasks/send`. Вызванный агент переходит по состояниям; клиенты подписываются на обновления через SSE или опрашивают сами.

> 🎒 **На пальцах.** Схема выше — это заказ в мастерской: приняли, работаем, «а какой цвет вам нужен?», готово. Посчитайте состояния: их семь, из них четыре финальных (completed, failed, canceled, rejected) — то есть задача всегда заканчивается ровно в одном из четырёх исходов, и цикл через input_required — единственный способ вернуться в working.

### Messages and Parts

Сообщение несёт одну или несколько Parts:

- `text` — обычный текст.
- `file` — blob в base64 с указанным mimeType.
- `data` — типизированный JSON (структурированный вход для вызываемого агента).

Пример:

```json
{
  "role": "user",
  "parts": [
    {"type": "text", "text": "Summarize this paper."},
    {"type": "file", "file": {"name": "paper.pdf", "mimeType": "application/pdf", "bytes": "..."}},
    {"type": "data", "data": {"targetLength": "3 paragraphs"}}
  ]
}
```

> 🎒 **На пальцах.** Одно сообщение — три вида груза, как посылка с описью. В примере частей ровно три: текст «Summarize this paper.», файл `paper.pdf` и данные `{"targetLength": "3 paragraphs"}`. Обратите внимание, зачем нужен `data`: длину саммари можно было впихнуть в текст, но тогда агенту пришлось бы её выпарсивать из фразы вместо чтения поля.

### Artifacts

Результаты — это Artifacts, а не сырые строки. Artifact — это именованный типизированный выход:

```json
{
  "name": "summary",
  "parts": [{"type": "text", "text": "..."}],
  "mimeType": "text/markdown"
}
```

Artifacts можно стримить кусками. Вызывающая сторона накапливает их у себя.

> 🎒 **На пальцах.** Разница между Artifact и обычной строкой — как между файлом с именем и запиской на салфетке. У артефакта из примера есть `name: "summary"` и `mimeType: "text/markdown"`, поэтому вызывающий сразу знает: это markdown, его можно отрендерить. Сырая строка заставила бы угадывать формат.

### Two transport bindings

1. **JSON-RPC over HTTP.** Эндпоинт `/a2a`, POST для запросов, опциональный SSE для стриминга. Привязка по умолчанию.
2. **gRPC.** Для корпоративных окружений, где gRPC и так родной.

Обе привязки несут одну и ту же логическую форму сообщения.

> 🎒 **На пальцах.** Две привязки — два способа доставить одно и то же письмо: курьером или по пневмопочте. Содержимое конверта не меняется. JSON-RPC over HTTP берут по умолчанию, потому что он работает из браузера и любого языка; gRPC берут там, где вся внутренняя сеть уже на gRPC и переучиваться дороже, чем добавить вторую привязку.

### Opacity preservation

Ключевой принцип дизайна: внутреннее состояние вызванного агента непрозрачно. Вызывающий видит состояние задачи и артефакты. Цепочка рассуждений вызванного агента, его вызовы инструментов, его делегирование под-агентам — всё это невидимо. Здесь отличие от MCP, где вызовы инструментов прозрачны.

Зачем так: A2A позволяет конкурентам сотрудничать, не раскрывая внутренностей. A2A умеет «вызови вот этого агента поддержки» так, что вызывающий не узнает, как этот агент устроен внутри.

> 🎒 **На пальцах.** Это как заказ в ресторане: вам выносят блюдо, а на кухню не пускают. Сравните с MCP из фазы 11: там host видит каждый вызов инструмента и его аргументы. В A2A вызывающий видит две вещи — состояние задачи и артефакт. Именно поэтому два конкурирующих банка могут связать своих агентов, не показывая друг другу ни промптов, ни моделей.

### Timeline

- **2025-04-09.** Google анонсирует A2A.
- **2025-06-23.** Протокол передан Linux Foundation.
- **2025-08.** Вбирает в себя ACP от IBM.
- **2025-09.** Выходит расширение AP2 (Agent Payments).
- **2026-04.** Релиз v1.0, 150+ поддерживающих организаций.

> 🎒 **На пальцах.** Посчитайте темп по таблице: от анонса (апрель 2025) до v1.0 (апрель 2026) прошёл ровно год, а до передачи в Linux Foundation — всего два с половиной месяца. Такая скорость передачи в фонд — сигнал, что Google сразу целилась в отраслевой стандарт, а не в свой формат: конкуренты не встраивают чужой протокол, если владелец один.

### Relationship to MCP

| Dimension | MCP | A2A |
|-----------|-----|-----|
| Use case | Агент — инструмент | Агент — агент |
| Opacity | Прозрачные вызовы инструментов | Непрозрачные внутренние рассуждения |
| Typical caller | Рантайм агента | Другой агент |
| State | Результат вызова инструмента | Task с жизненным циклом |
| Authorization | OAuth 2.1 (Phase 13 · 16) | Agent Card, подписанная JWT (AP2) |
| Transport | Stdio / Streamable HTTP | JSON-RPC over HTTP / gRPC |

Берите MCP, когда нужно вызвать конкретный инструмент. Берите A2A, когда нужно передать другому агенту целую задачу. Многие продакшен-системы используют оба: MCP — для слоя инструментов, A2A — для слоя сотрудничества.

```figure
a2a-task-lifecycle
```

> 🎒 **На пальцах.** Прогоните таблицу как чек-лист выбора. Нужно «прочитай строку из Postgres» — это конкретный инструмент, берём MCP. Нужно «подготовь квартальный отчёт по этим данным» — это целая задача с непредсказуемым числом шагов, берём A2A. В реальной системе обычно оба слоя: агент внутри дергает MCP, а снаружи принимает Task по A2A.

## Use It

`code/main.py` реализует минимальную обвязку A2A: агент-исследователь публикует свою карточку, агент-писатель получает `tasks/send` с частями, среди которых PDF и текстовая инструкция, проходит переходы working → input_required → working → completed и возвращает текстовый артефакт. Всё на stdlib; транспорт в памяти, чтобы сосредоточиться на форме сообщений.

На что смотреть:

- Форма JSON у Agent Card.
- Присвоение id задачи и переходы состояний.
- Сообщения с частями разных типов.
- Ветка input-required посреди задачи.
- Возврат артефакта при завершении.

> 🎒 **На пальцах.** В `code/main.py` переходов ровно четыре: working → input_required → working → completed. Обратите внимание на второй: агент честно останавливается и спрашивает, вместо того чтобы догадываться. Это и есть главный подарок жизненного цикла — пауза за уточнением легальна, а не ошибка таймаута.

## Ship It

Этот урок даёт `outputs/skill-a2a-agent-spec.md`. По описанию нового агента, которого должны уметь вызывать другие агенты, навык выдаёт JSON для Agent Card, схему skills и чертёж эндпоинта.

> 🎒 **На пальцах.** Навык делает скучную, но обязательную часть: карточку, схему skills и чертёж эндпоинта. Скучную — потому что одна опечатка в `inputModes` ломает всех вызывающих сразу, а не одного. Автогенерация карточки из описания дешевле, чем править её руками в пяти местах.

## Exercises

1. Запустите `code/main.py`. Проследите полный жизненный цикл Task, включая паузу input-required, где вызванный агент просит уточнение.

2. Добавьте подписанную Agent Card. Подпишите её HMAC по канонической форме JSON карточки. Напишите проверяющую функцию и убедитесь, что на изменённой карточке она падает.

3. Реализуйте стриминг задачи: агент-писатель отдаёт три инкрементальных куска артефакта через SSE, а вызывающий их накапливает.

4. Спроектируйте A2A-агента, который оборачивает MCP-сервер. Сопоставьте каждый MCP-инструмент со skill в A2A. Отметьте компромиссы — какая непрозрачность теряется?

5. Прочитайте анонс A2A v1.0 и найдите ту единственную возможность, которую на апрель 2026 года не реализовал ни один фреймворк. (Подсказка: она связана с многошаговым делегированием задач.)

> 🎒 **На пальцах.** Во втором задании ловушка не в HMAC, а в слове «каноническая». `{"a":1,"b":2}` и `{"b":2,"a":1}` — одна и та же карточка, но разные байты, значит разные подписи. Поэтому перед подписью надо отсортировать ключи и убрать лишние пробелы (`json.dumps(card, sort_keys=True, separators=(",", ":"))`), иначе проверка будет падать на честных карточках.

## Key Terms

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| A2A | «Протокол агент — агент» | Открытый протокол для сотрудничества непрозрачных агентов |
| Agent Card | «`.well-known/agent.json`» | Опубликованные метаданные с описанием skills агента и его эндпоинта |
| Skill | «Вызываемая единица» | Именованная операция, которую поддерживает агент (аналог инструмента в MCP) |
| Task | «Единица делегирования» | Рабочий элемент с жизненным циклом и итоговым артефактом |
| Message | «Вход задачи» | Несёт в себе Parts (text, file, data) |
| Part | «Типизированный кусок» | Элемент сообщения типа `text` / `file` / `data` |
| Artifact | «Выход задачи» | Именованный типизированный результат, возвращаемый при завершении |
| AP2 | «Agent Payments Protocol» | Расширение с подписанными Agent Card для доверия и платежей |
| Opacity | «Сотрудничество вслепую» | Внутренности вызванного агента скрыты от вызывающего |
| Input-required | «Пауза задачи» | Состояние цикла, когда агенту нужны дополнительные данные |

## Further Reading

- [a2a-protocol.org](https://a2a-protocol.org/latest/) — каноническая спецификация A2A
- [a2aproject/A2A — GitHub](https://github.com/a2aproject/A2A) — референсные реализации и SDK
- [Linux Foundation — A2A launch press release](https://www.linuxfoundation.org/press/linux-foundation-launches-the-agent2agent-protocol-project-to-enable-secure-intelligent-communication-between-ai-agents) — передача управления в июне 2025-го
- [Google Cloud — A2A protocol upgrade](https://cloud.google.com/blog/products/ai-machine-learning/agent2agent-protocol-is-getting-an-upgrade) — роадмап и рост числа партнёров
- [Google Dev — A2A 1.0 milestone](https://discuss.google.dev/t/the-a2a-1-0-milestone-ensuring-and-testing-backward-compatibility/352258) — заметки к релизу v1.0 и советы по обратной совместимости
