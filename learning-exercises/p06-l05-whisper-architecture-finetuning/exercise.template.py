"""
Whisper: архитектура и дообучение

Реализуй функции ниже. Заготовки бросают NotImplementedError — удали
строку raise и напиши код.

Правила:
  * сторонние библиотеки не использовать, только стандартная (math, random).
    Смысл упражнения — собрать руками.
  * файл test_exercise.py не трогай.
  * эталон лежит в solution.py — открывай ПОСЛЕ своих зелёных тестов.

Запуск:  ./learning-exercises/watch.sh p06-l05-whisper-architecture-finetuning
Разбор:  /check-code p06-l05-whisper-architecture-finetuning
"""

import math


def pad_or_trim(signal, target_len=480000):
    """Привести клип ровно к окну Whisper: короткий добить нулями, длинный обрезать.

    480000 отсчётов = 30 секунд при 16 кГц. Это жёсткий вход энкодера, а не
    рекомендация: сеть принимает ровно столько и ничего другого.

    pad_or_trim([1.0, 2.0, 3.0], 5)  ->  [1.0, 2.0, 3.0, 0.0, 0.0]
    pad_or_trim([1.0, 2.0, 3.0], 2)  ->  [1.0, 2.0]
    pad_or_trim([], 3)               ->  [0.0, 0.0, 0.0]

    Ловушка: длинный клип именно ОБРЕЗАЕТСЯ, а не сжимается. Ресемплить его
    в 30 секунд — значит поднять высоту голоса и получить мусор на выходе.

    Вторая ловушка: исходный список править нельзя, возвращай новый.

    Зачем в AI: это whisper.pad_or_trim. И именно из-за дополнения нулями
    двухсекундный клип, растянутый до 30 секунд тишины, любит дописывать
    «Thanks for watching!» — сеть учили на субтитрах с YouTube.
    """
    raise NotImplementedError


def frame_budget(duration_s, sr=16000, hop=160, conv_stride=2):
    """Сколько кадров получит энкодер: вернуть кортеж (кадров мел, кадров энкодера).

    Кадров мел-спектрограммы: duration_s * sr / hop. Свёртка на входе
    энкодера идёт с шагом conv_stride и во столько же раз прореживает время.

    frame_budget(30.0)   ->  (3000, 1500)   (канонический бюджет Whisper)
    frame_budget(1.0)    ->  (100, 50)
    frame_budget(0.5)    ->  (50, 25)

    hop=160 при sr=16000 — это ровно 10 мс на кадр, стандарт ASR.

    Ловушка: считать надо через hop, а не через длину окна. Окна перекрываются
    (25 мс окно, 10 мс шаг), и по длине окна кадров вышло бы втрое меньше.

    Зачем в AI: 1500 кадров энкодера — это длина, на которую cross-attention
    декодера смотрит. Отсюда и стоимость внимания, и то, почему Turbo режет
    декодер, а не энкодер.
    """
    raise NotImplementedError


def normalize_log_mel(mel_power):
    """Применить точное Whisper-преобразование к mel power одного клипа.

    Сначала каждое значение зажимается снизу на 1e-10 и берётся log10. Затем
    динамический диапазон зажимается ПО ЭТОМУ КЛИПУ на уровне peak - 8.0, а
    фиксированное преобразование (x + 4.0) / 4.0 переводит признаки примерно
    в [-1, 1]. Статистики корпуса, mean и std здесь вообще не участвуют.

    normalize_log_mel([[1.0, 1e-8, 1e-10]])  ->  [[1.0, -1.0, -1.0]]
    normalize_log_mel([[1e-4]])               ->  [[0.0]]

    Ловушка: обычная стандартизация по mean/std — другой preprocessing. Код
    отработает без ошибки, но Whisper увидит признаки не из обучающего
    распределения и качество распознавания рухнет.

    Зачем в AI: это буквально clamp-and-affine из
    whisper.audio.log_mel_spectrogram; менять его при fine-tuning нельзя.
    """
    raise NotImplementedError


def build_prompt(language, task="transcribe", timestamps=False):
    """Собрать промпт декодера Whisper: список специальных токенов.

    Порядок жёсткий: startoftranscript, тег языка, тег задачи и, если
    timestamps выключены, notimestamps.

    build_prompt("en")
        ->  ["<|startoftranscript|>", "<|en|>", "<|transcribe|>", "<|notimestamps|>"]
    build_prompt("fr", task="translate", timestamps=True)
        ->  ["<|startoftranscript|>", "<|fr|>", "<|translate|>"]

    task принимает только "transcribe" и "translate"; на любом другом
    значении подними ValueError — молча свалиться на «transcribe» хуже,
    потому что модель будет делать не то, о чём её просили.

    Зачем в AI: одна модель делает много задач именно потому, что задача
    задаётся токенами в промпте, а не отдельной головой. Поменял <|en|> на
    <|fr|> — та же сеть транскрибирует французский.
    """
    raise NotImplementedError


def parse_prompt(tokens):
    """Разобрать промпт обратно: словарь с ключами language, task, timestamps.

    parse_prompt(["<|startoftranscript|>", "<|de|>", "<|translate|>"])
        ->  {"language": "de", "task": "translate", "timestamps": True}

    Первый токен обязан быть <|startoftranscript|>, второй — тегом языка.
    Иначе ValueError: промпт без SOT декодер не примет.

    Обратна к build_prompt — на этом свойстве её и проверяют: собрать и
    разобрать обратно обязано дать те же параметры.

    Зачем в AI: половина багов с Whisper — это «модель почему-то переводит
    вместо транскрипции». Умение прочитать промпт закрывает вопрос за минуту.
    """
    raise NotImplementedError


def chunk_schedule(duration_s, chunk_s=30.0, stride_s=5.0):
    """Расписание окон для длинного клипа: список пар (начало, конец) в секундах.

    Окна длиной chunk_s идут с перекрытием stride_s, то есть шаг равен
    chunk_s - stride_s. Последнее окно обрезается по концу записи.

    chunk_schedule(10.0)             ->  [(0.0, 10.0)]
    chunk_schedule(70.0)             ->  [(0.0, 30.0), (25.0, 55.0), (50.0, 70.0)]
    chunk_schedule(0.0)              ->  []

    stride_s обязан быть меньше chunk_s, иначе шаг нулевой или отрицательный
    и цикл никогда не кончится — подними ValueError.

    Ловушка: перекрытие нужно не «на всякий случай». Слово, разрезанное
    границей окна, теряется в обоих окнах сразу; перекрытие даёт ему шанс
    целиком попасть хотя бы в одно.

    Зачем в AI: у Whisper жёсткое окно 30 секунд, и весь long-form
    (WhisperX, faster-whisper) построен вокруг такого расписания.
    """
    raise NotImplementedError


def merge_chunk_transcripts(chunks):
    """Склеить расшифровки перекрывающихся окон, убрав повтор на стыке.

    На вход список строк — по одной на окно из chunk_schedule. Для каждой
    следующей строки ищем САМЫЙ ДЛИННЫЙ хвост уже склеенного текста, который
    совпадает с началом новой строки (по словам), и выбрасываем его из новой.

    merge_chunk_transcripts(["turn on the", "on the light"])
        ->  "turn on the light"
    merge_chunk_transcripts(["hello world", "goodbye"])
        ->  "hello world goodbye"
    merge_chunk_transcripts([])
        ->  ""

    Ловушка: сравнивать надо по словам, а не по символам. По символам
    «the light» и «then» дадут ложное совпадение на «the».

    Зачем в AI: перекрытие окон, которое спасало слова на границе, теперь
    даёт дубли. Без этой склейки в расшифровке часового подкаста будет
    сотня повторённых фраз.
    """
    raise NotImplementedError


def lora_parameter_count(module_shapes, r):
    """Сколько параметров добавляет LoRA: сумма r * (d_in + d_out) по модулям.

    module_shapes — список пар (d_out, d_in) тех матриц, к которым цепляются
    адаптеры (обычно q_proj и v_proj каждого слоя внимания).

    lora_parameter_count([(1280, 1280)], 16)  ->  40960
    lora_parameter_count([(1280, 1280)], 0)   ->  0
    lora_parameter_count([], 16)              ->  0

    Смысл: вместо матрицы d_out x d_in обучаются две тонкие — B размера
    (d_out, r) и A размера (r, d_in). При r=16 и d=1280 это 40 тысяч чисел
    вместо 1.6 миллиона.

    Ловушка: складывать, а не перемножать. r * d_in * d_out — это уже размер
    полной матрицы, помноженный на ранг, то есть ХУЖЕ, чем без LoRA.

    Зачем в AI: LoRA на Whisper-Turbo даёт ~3M обучаемых параметров против
    809M полных, память под оптимизатор падает вчетверо, WER растёт меньше
    чем на 0.3.
    """
    raise NotImplementedError
