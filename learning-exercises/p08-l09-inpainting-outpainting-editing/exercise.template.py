"""
Инпейнтинг, аутпейнтинг и редактирование изображений

Реализуй функции ниже. Заготовки бросают NotImplementedError — удали
строку raise и напиши код.

Правила:
  * сторонние библиотеки не использовать, только стандартная (math, random).
    Смысл упражнения — собрать руками.
  * файл test_exercise.py не трогай.
  * эталон лежит в solution.py — открывай ПОСЛЕ своих зелёных тестов.

Запуск:  ./learning-exercises/watch.sh p08-l09-inpainting-outpainting-editing
Разбор:  /check-code p08-l09-inpainting-outpainting-editing
"""

import math


def invert_mask(mask):
    """Развернуть маску: True <-> False.

    invert_mask([True, False, False])  ->  [False, True, True]

    Соглашение на весь урок: mask[i] == True означает «это место надо
    перегенерировать», False — «это место известно и трогать его нельзя».

    Зачем функция: аутпейнтинг — это ровно инпейнтинг с перевёрнутой маской.
    Никакой отдельной модели и никакого отдельного лосса не нужно.
    """
    raise NotImplementedError


def dilate_mask(mask, radius):
    """Расширить маску на radius позиций в каждую сторону.

    dilate_mask([False, True, False, False], 1)  ->  [True, True, True, False]
    dilate_mask([False, True, False, False], 0)  ->  [False, True, False, False]

    Позиция попадает в новую маску, если хоть одна True-позиция исходной маски
    оказалась не дальше radius.

    Зачем: главный артефакт наивного инпейнтинга — видимый шов по границе
    маски. Лечится расширением маски на 8-16 пикселей: модель перерисовывает
    и саму границу, шить нечего.

    Ловушка: у краёв массива окно вылезает за пределы. Обрезай индексы, а не
    полагайся на отрицательные — mask[-1] в Python это последний элемент, и
    маска «замкнётся в кольцо».
    """
    raise NotImplementedError


def build_inpaint_input(noisy_latent, encoded_source, mask):
    """Вход «правильной» инпейнт-модели: конкатенация трёх частей.

    [ noisy_latent | encoded_source | mask как 0/1 ]

    build_inpaint_input([0.5], [2.0], [True])   ->  [0.5, 2.0, 1.0]
    build_inpaint_input([0.5], [2.0], [False])  ->  [0.5, 2.0, 0.0]

    В настоящем SD-Inpaint это 9 каналов вместо 4: 4 канала зашумлённого
    латента, 4 канала закодированного исходника и 1 канал маски. Отсюда и
    название «9-channel U-Net».

    Смысл именно в среднем блоке: модель ВИДИТ, что находится вокруг дырки,
    и потому дописывает согласованную текстуру. Без него остаётся только
    наивная переинъекция, которая оставляет швы.
    """
    raise NotImplementedError


def reinject_known(x_t, mask, clean, t, alpha_bars, rng):
    """Наивный инпейнтинг: известные позиции заменить на зашумлённый исходник.

    Для каждой позиции с mask[i] == False:
        x_t[i] = sqrt(alpha_bar_t) * clean[i] + sqrt(1 - alpha_bar_t) * z

    Позиции с mask[i] == True не трогать вовсе — их считает сам денойзер.

    reinject_known([9.0, 9.0], [True, False], [0.0, 5.0], 0, [1.0], rng)
        ->  [9.0, 5.0]      (при alpha_bar = 1 шума нет, известное встаёт как есть)

    Уровень шума обязан совпадать с текущим шагом обратной цепочки: если
    подставить чистый clean в наполовину зашумлённый x_t, распределения не
    сойдутся и модель увидит невозможную картинку.

    Ловушка: x_t менять на месте нельзя, верни новый список. И rng трогай
    ровно столько раз, сколько известных позиций — иначе воспроизводимость
    поедет.
    """
    raise NotImplementedError


def restore_known(x, mask, clean):
    """Финальный шаг: вернуть известные позиции ровно как в исходнике.

    restore_known([1.0, 2.0], [True, False], [9.0, 9.0])  ->  [1.0, 9.0]

    Это обещание, которое инпейнтинг даёт клиенту: за пределами маски картинка
    БИТ-В-БИТ та же. Даже наилучший денойзер накопит на известных позициях
    крошечную ошибку, поэтому в конце их просто прибивают гвоздями.

    Тоже без мутации входа.
    """
    raise NotImplementedError


def sdedit_start_step(strength, T):
    """Сколько шагов зашумления делает SDEdit при заданной силе правки.

    sdedit_start_step(0.3, 1000)  ->  300
    sdedit_start_step(0.0, 1000)  ->  0      (картинка не меняется вообще)
    sdedit_start_step(1.0, 1000)  ->  1000   (генерация почти с нуля)

    Ровно так считает diffusers: init_timestep = num_inference_steps * strength.

    Это единственная ручка SDEdit, и она обменивает верность исходнику на
    свободу правки: 0.3 — лёгкая стилизация, 0.6 — сохраняется только крупная
    структура, 0.9 — от исходника почти ничего.

    Значения вне [0, 1] надо зажать, а не пропускать: отрицательное число
    шагов бессмысленно.
    """
    raise NotImplementedError


def sdedit_noise(x0, steps, alpha_bars, rng):
    """Зашумить картинку на steps шагов вперёд — первая половина SDEdit.

    steps — то, что вернул sdedit_start_step. При steps == 0 шума нет вообще
    и возвращается копия x0.

    sdedit_noise([1.0, 2.0], 0, [0.9], rng)  ->  [1.0, 2.0]

    Для steps >= 1 берётся alpha_bars[steps - 1]: сделали steps шагов, значит
    накопили произведение первых steps множителей.

    Вторая половина SDEdit — прогнать обратную цепочку с НОВЫМ промптом от
    этого уровня шума до нуля. Обучать ничего не надо, поэтому img2img работает
    на любом чекпоинте.
    """
    raise NotImplementedError


def repaint_timesteps(T, jump_every, jump_length):
    """Расписание шагов RePaint: спуск с возвратами наверх. Список посещённых t.

    repaint_timesteps(5, 2, 0)  ->  [4, 3, 2, 1, 0]          (обычный спуск)
    repaint_timesteps(5, 2, 1)  ->  [4, 3, 3, 2, 2, 1, 1, 0]

    Идея Lugmayr et al.: каждые jump_every шагов вниз откатиться на
    jump_length шагов вверх и переденойзить этот участок заново. Известная и
    сгенерированная части успевают «договориться», шов размывается. Обученная
    инпейнт-модель для этого не нужна.

    Требование jump_length < jump_every обязательно: иначе спуск в среднем не
    спускается и цикл никогда не кончится. Проверь его явно и брось ValueError,
    а не жди зависания.
    """
    raise NotImplementedError
