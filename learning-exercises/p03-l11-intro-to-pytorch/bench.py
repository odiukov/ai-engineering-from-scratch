"""Входные данные для замера скорости.

mse_loss и sgd_step принимают Tensor, а он у каждого свой — и в
solution.py, и в exercise.py. Подсунуть один и тот же объект обеим
версиям нельзя, поэтому меряем то, что работает на голых списках:
randn и полный цикл обучения fit_line, который внутри дёргает всё
остальное — matmul, add, backward и шаг оптимизатора.
"""

import random

random.seed(0)          # обязательно: замер должен быть воспроизводим

BENCH = {
    "randn": (60, 60, 0, 1.0),
    "fit_line": (0, 60, 0.05, 3.0, -1.0),
}
